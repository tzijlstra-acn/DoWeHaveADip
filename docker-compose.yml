version: "3.9"
services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data/cache:/app/data/cache
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
    restart: unless-stopped
